// Platform-neutral core: the renderer, colour palette and 2D drawing/annotation API.
// Deliberately has NO Minecraft dependency so any platform adapter can build on it.
// All shared configuration is inherited from the root build.gradle `subprojects` block.
